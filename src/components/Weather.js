import React from 'react'
const Weather = ({location}) => {
    return (
        <div>
            Weather comp: {location?.longitude}
            testing
        </div>
    )
}

export default Weather;